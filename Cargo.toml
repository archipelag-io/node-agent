[package]
name = "archipelag-agent"
version = "0.4.0"
edition = "2021"
description = "Island software for the Archipelag.io distributed compute network"
authors = ["archipelag.io <hello@archipelag.io>"]
license = "MIT"

[[bin]]
name = "island"
path = "src/main.rs"

[features]
default = []
pipeline = ["dep:quinn", "dep:rcgen", "dep:llama-cpp-sys-2", "dep:base64", "dep:hostname"]
onnx = ["dep:ort", "dep:ndarray", "dep:tokenizers", "dep:image", "dep:base64"]
gguf = ["dep:llama-cpp-2", "dep:encoding_rs"]
diffusers = ["dep:candle-core", "dep:candle-transformers", "dep:candle-nn", "dep:tokenizers", "dep:image", "dep:base64", "dep:hf-hub"]
# Exo multi-Island clustering. Delegates sharding to a local `exo` subprocess and
# talks to it over HTTP, so it needs no native ML deps (reqwest/tokio/serde only).
exo = []
all-runtimes = ["onnx", "gguf", "diffusers", "pipeline", "exo"]

[dependencies]
# Async runtime
tokio = { version = "1", features = ["full"] }

# Docker client
bollard = "0.18"

# WASM runtime
wasmtime = "27"
wasmtime-wasi = "27"

# NATS client
async-nats = "0.38"

# TLS crypto provider (required for rustls when connecting to NATS over TLS)
rustls = { version = "0.23", features = ["ring"] }

# Serialization
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# CLI
clap = { version = "4", features = ["derive"] }

# Configuration
config = "0.14"

# Logging
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }

# Error handling
anyhow = "1"
thiserror = "2"

# Cryptographic hashing for verification
sha2 = "0.10"
hex = "0.4"

# Ed25519 signature verification for updates
ed25519-dalek = { version = "2", features = ["rand_core"] }

# Version comparison
semver = "1"

# UUID for host/job IDs
uuid = { version = "1", features = ["v4", "serde"] }

# Futures utilities
futures-util = "0.3"

# Concurrent data structures
dashmap = "6"

# Random number generation
rand = "0.8"

# System info
num_cpus = "1.16"
sysinfo = "0.32"
reqwest = { version = "0.13.1", features = ["stream", "json"] }
dirs = "6.0.0"

# OCI image pulling (Docker-free container support)
oci-distribution = { version = "0.11", default-features = false, features = ["rustls-tls"] }
oci-spec = "0.7"
tar = "0.4"
flate2 = "1"
# File time manipulation (for model cache LRU)
filetime = "0.2"

# ONNX Runtime (optional, feature-gated)
ort = { version = "2.0.0-rc.12", optional = true }
ndarray = { version = "0.16", optional = true }

# GGUF/llama.cpp (optional, feature-gated)
llama-cpp-2 = { version = "0.1.139", optional = true, default-features = false }
llama-cpp-sys-2 = { version = "0.1.139", optional = true, default-features = false }
encoding_rs = { version = "0.8", optional = true }

# Diffusers via Candle (optional, feature-gated)
candle-core = { version = "0.9.2", optional = true }
candle-transformers = { version = "0.9.2", optional = true }
candle-nn = { version = "0.9.2", optional = true }
tokenizers = { version = "0.20", optional = true, default-features = false, features = ["onig"] }

# HuggingFace Hub client for model downloads (diffusers)
hf-hub = { version = "0.4", optional = true }

# Image processing (ONNX vision + diffusers output)
image = { version = "0.25", optional = true }
base64 = { version = "0.22", optional = true }

# QUIC transport for pipeline activations (optional, feature-gated)
quinn = { version = "0.11", optional = true }
rcgen = { version = "0.13", optional = true }
hostname = { version = "0.4", optional = true }

# GPU detection (NVIDIA)
# nvml-wrapper = "0.9"  # Uncomment when needed

[dev-dependencies]
tokio-test = "0.4"
tempfile = "3"
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "message_parsing"
harness = false

[[bench]]
name = "heartbeat_serialization"
harness = false

[profile.release]
lto = true
strip = true
codegen-units = 1
